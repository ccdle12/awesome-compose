global: {}

route:
  receiver: 'null-receiver'
  group_by: ['alertname', 'job']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'null-receiver'
  # This webhook configuration is intentionally fake,
  #     # but it satisfies the requirement for a receiver.
    webhook_configs:
    - url: 'http://127.0.0.1:9999/dev/null'
